// List three reserved words in JavaScript and create a valid use case for each.

let count =  5;     //reserve word is let
if (count>0){
    console.log("count is more than zero");
    
}

function m(a:number, b:number) {    //reserve word is return
    return a+b
}

