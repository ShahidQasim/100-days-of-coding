// Write a JavaScript object and convert it into a JSON string.
const credentials = {
    name: "shahid",
    age: 29,
    school: "usmania"
};
let tojson = JSON.stringify(credentials);
console.log(tojson);
export {};
///console.log(JSON.stringify(credentials));
