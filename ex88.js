// Converting Strings to Numbers:
// Write a function that takes a string representation of a number (e.g., `123`) and 
//converts it into an actual number type.
function main(styy) {
    return Number(styy);
}
console.log(main("12345"));
export {};
